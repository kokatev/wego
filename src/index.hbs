<!doctype html>
<html lang="en">
  <head>
    <title>
      {{#if title}}
        {{title}}
      {{else}}
        TVS Wego
      {{/if}}
    </title>
    {{> styles}}

  </head>
  <body>
    {{> nav }}

    {{> hero }}

    {{> tabs }}

    {{> footer }}

    {{> scripts }}
  </body>
</html>
